'use strict';

describe('task-mule', function () {

	var taskMule = require('../index.js');

	it('task-mule can handle an unhandled exception and fail the task', function () {

		taskMule({

			});


	});

});